#include <stdio.h>
#include <string.h>
#include <time.h>

#include "utils.h"

BlockType spawn(Block* b, BlockType t);
void spawnI(Block* b);
void spawnO(Block* b);
void spawnT(Block* b);
void spawnZ(Block* b);
void spawnS(Block* b);
void spawnJ(Block* b);
void spawnL(Block* b);

